import styled from "styled-components";

const Container = styled.div``;

export const Header = () => {
    return (
        <Container>
            <h1>Relative Intensity Calculator</h1>
        </Container>
    )

}
